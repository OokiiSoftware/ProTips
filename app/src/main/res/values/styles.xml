<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="fontFamily">@font/bebasneue_regular</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="titleTextColor">@color/text_dark</item>

        <item name="textColor">@color/text_dark</item>
        <item name="backgroundColor">@color/colorPrimaryLight_3</item>
        <item name="toolbarHeight">@dimen/toolbar_height</item>

    </style>

    <style name="AppTheme.botao_notificacao" parent="Theme.AppCompat.Light">
        <item name="android:colorControlHighlight">@android:color/white</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="EditText.Login" parent="Base.Widget.AppCompat.EditText">
        <item name="android:paddingTop">@dimen/et_top_bot</item>
        <item name="android:paddingBottom">@dimen/et_top_bot</item>
        <item name="android:paddingStart">@dimen/et_start_end</item>
        <item name="android:paddingEnd">@dimen/et_start_end</item>
        <item name="android:background">@drawable/bg_stroke_edittext_login</item>
        <item name="android:textColor">@color/text_dark</item>
        <item name="android:textColorHint">@color/text_dark</item>
        <item name="android:layout_marginTop">@dimen/login_item_margin</item>
    </style>
    <style name="EditText.NovoTip" parent="Base.Widget.AppCompat.EditText">
        <item name="android:padding">@dimen/et_novo_tip</item>
        <item name="android:background">@drawable/bg_stroke_primary_color</item>
        <item name="android:textColor">@color/text_light</item>
        <item name="android:textColorHint">@color/text_light</item>
    </style>

</resources>
